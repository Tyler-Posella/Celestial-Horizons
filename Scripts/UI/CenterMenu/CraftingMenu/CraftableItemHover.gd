class_name CraftableItemHover
extends Control

# Variables
var craftable_item_dictionary

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass
	#for i in 3:
	#	var scene = load("res://Scenes/UI/CenterMenu/CraftingMenu/CraftableItemIngredient.tscn")
	#	var new = scene.instantiate()
	#	new.item = load("res://Resoures/Fruit/Apple.tres")
	#	$AspectRatioContainer/GridContainer.add_child(new)


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
